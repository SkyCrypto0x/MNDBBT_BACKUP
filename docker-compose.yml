version: "3.9"

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  buybot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "3000:3000"
